// Importamos el metodo para configurara la store desde os reducer:
import { configureStore } from "@reduxjs/toolkit";

// Creamos la store desde los reducer:
export const store = configureStore({
  reducer: {},
});
